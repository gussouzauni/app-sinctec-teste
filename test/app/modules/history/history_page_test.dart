import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_sintec/app/modules/history/history_page.dart';

void main() {
  testWidgets('HistoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HistoryPage(title: 'History')));
    //  final titleFinder = find.text('History');
    //  expect(titleFinder, findsOneWidget);
  });
}
